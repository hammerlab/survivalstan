[TYPECHECK]
# Without ignoring this, we get errors like:
# E:249,20: Module 'numpy' has no 'nan' member (no-member)
ignored-modules = numpy, finfo, numpy.random, patsy, matplotlib.cm
ignored-classes = numpy, finfo, patsy, matplotlib
